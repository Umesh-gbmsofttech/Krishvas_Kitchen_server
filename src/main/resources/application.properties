spring.application.name=Krishva-s-Kitchen

server.address=0.0.0.0
server.port=${PORT:8080}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/krishvas_kitchen?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}
spring.datasource.hikari.maximum-pool-size=${DB_POOL_SIZE:5}
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.initialization-fail-timeout=60000
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
logging.level.org.hibernate.SQL=warn
spring.jpa.open-in-view=false

app.jwt.secret=${JWT_SECRET:change-this-to-a-long-dev-secret-1234567890}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:604800000}
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:*}

app.admin.email=${ADMIN_EMAIL:admin@gmail.com}
app.admin.password=${ADMIN_PASSWORD:Admin@123}
app.admin.name=${ADMIN_NAME:Krishva}

api.keys.map.ors=${ORS_MAP:jhjhj}
api.keys.map.tiler=${MAP_TILER:hjhjh}

stripe.secret.key=${STRIPE_SECRET_KEY:sk_test_51}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET:whsec_J5h6}
stripe.currency=${STRIPE_CURRENCY:gbp}

spring.jackson.time-zone=Asia/Kolkata
management.endpoints.web.exposure.include=health,info

app.image.max-size-bytes=10485760

# app.redis.enabled=${APP_REDIS_ENABLED:false}
# spring.data.redis.host=${REDIS_HOST:localhost}
# spring.data.redis.port=${REDIS_PORT:6379}

app.redis.enabled=false
spring.data.redis.host=localhost
spring.data.redis.port=6379


# stripe listen --forward-to http://localhost:8080/api/stripe/webhook

# test card: 4242 4242 4242 4242
